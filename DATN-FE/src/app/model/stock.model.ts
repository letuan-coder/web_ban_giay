export interface Stock {
  id: number;
  sku: string; // Assuming Stock has a name, similar to Brand/Category
  quantity: number; // Example property
  actualReceivedDate:string;
  // Add other relevant stock properties
}
